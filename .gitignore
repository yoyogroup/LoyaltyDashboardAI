# Lambda dependencies — installed by `npm ci` before `sam deploy`
backend/src/node_modules/

# SAM build artifacts
backend/.aws-sam/
# Note: backend/samconfig.toml IS checked in — it pins stack/region for CI.
# Contains no secrets (keys live in Secrets Manager).

# Local env files (never commit)
.env
.env.*

# OS / editor
.DS_Store
.idea/
.vscode/

# Local Claude Code config (preview server etc.) — per-machine, not portable
.claude/
